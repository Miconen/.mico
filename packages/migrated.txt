# Packages that MOVED to nix and should no longer be installed by pacman.
#
# bootstrap.sh removes any of these that are still present, but only after
# confirming, and only after home-manager has successfully activated - so the
# nix replacement is guaranteed to already be on PATH.
#
# Some entries (notably git/ripgrep) may be required by other pacman packages.
# bootstrap tries each removal independently and leaves dependency-blocked ones
# installed and explicit.

eza
fastfetch
git
# Not migrated so much as replaced: nix provides btop instead. Kept here so
# bootstrap still offers to remove a pacman-installed htop.
htop
keychain
opencode
jq
lazygit
neovim
wget
zoxide

# Never installed on the laptop, but present in the old WSL manifest
github-cli
hub
lazydocker
less
mise
nvm
ripgrep
unzip
zellij
fzf
